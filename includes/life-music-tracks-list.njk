---
pagination:
  data: shortTermTracks
  size: 10
  alias: pagination
---

{% include "head.njk" %}
<main id="content">
  <header class="intro s">
    <div>
      <nav class="breadcrumbs">
        <a href="/life/">Life</a>
        <a href="/life/listening/">Listening</a>
        <a href="/life/listening/music/">Music</a>
        <a href="/life/listening/music/tracks/">{{ title }}</a>
      </nav>
      <h1>{{ title }}</h1>
      {{ content | safe }}
      <p>This data was last updated {{ lifeDataLastUpdated["top-tracks.yml"].commit.author.date | datetimetime | safe }} in <a href="{{ lifeDataLastUpdated['top-tracks.yml'].html_url }}">{{ lifeDataLastUpdated["top-tracks.yml"].sha | truncate(6, true, "") }}</a> in <a href="https://github.com/AnandChowdhary/life-data/blob/master/top-tracks.yml">top-tracks.yml</a> using <a href="https://github.com/AnandChowdhary/services/blob/master/api/wakatime.ts">Wakatime</a> on GitHub.</p>
    </div>
  </header>
  <div class="container-outer">
    <section class="link-list music-list">
      {% for item in pagination %}
        <article>
          <a href="{{ item.href }}">
            <div class="f">
              {{ item.name }}
              {# <img alt="" src="{{ item.images[1].url }}">
              <div class="ff">
                <h2>{{ item.name }}</h2>
                <div>{{ item.genres[0] | capitalize }}</div>
              </div> #}
            </div>
          </a>
        </article>
      {% endfor %}
    </section>
    {% if pagination.pageLinks.length > 1 %}
    <nav class="pagination {% if pagination.previousPageLink %}pagination--has-prev{% endif %} {% if pagination.nextPageLink %}pagination--has-next{% endif %}">
      {% if pagination.previousPageLink %}
        <a class="pagination-item pagination-item--prev" href="{{ pagination.previousPageHref | url }}"><span>Previous</span></a>
      {% endif %}
      {% if pagination.nextPageLink %}
        <a class="pagination-item pagination-item--next" href="{{ pagination.nextPageHref | url }}"><span>Next</span></a>
      {% endif %}
    </nav>
    {% endif %}
  </div>
</main>
{% include "footer.njk" %}
